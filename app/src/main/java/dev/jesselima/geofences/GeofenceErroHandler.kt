package dev.jesselima.geofences

import android.content.Context


/**
 * Returns the error string for a geofencing error code.
 */
fun geofenceErrorHandler(context: Context, errorCode: Int): String {
    return ""
}
